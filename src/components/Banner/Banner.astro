---
import styles from "./Banner.module.css";
const palabras = [
  "DESARROLLAR", "INNOVAR", "IMPULSAR", "RESOLVER", "CREAR",
  "OPTIMIZAR", "AUTOMATIZAR", "CONECTAR", "INSPIRAR", "COLABORAR", "DISEÑAR", "REVOLUCIONAR"
];
---
<article class={styles.bannerContainer}>
  <div class={styles.marquee}>
    <div class={styles.track}>
      {Array(4).fill(palabras).flat().map((word) => (
        <span class={styles.word} >{word}</span>
      ))}
    </div>
  </div>
</article>
